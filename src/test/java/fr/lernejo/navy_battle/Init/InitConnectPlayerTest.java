package fr.lernejo.navy_battle.Init;
/*
import fr.lernejo.navy_battle.Controller;
import fr.lernejo.navy_battle.IController;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Spy;
import org.mockito.exceptions.base.MockitoException;
import org.mockito.junit.jupiter.MockitoExtension;

import java.net.UnknownHostException;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class InitConnectPlayerTest {

    @Spy
    IController controller = mock(Controller.class);

    @InjectMocks
    InitConnectPlayer initConnectPlayer = new InitConnectPlayer();

    @Test
    void launch() {
        try{
            doNothing().when(controller).StartServer();
            doNothing().when(controller).StartGameWithConnexion();
            initConnectPlayer.launch(4752, "http://localhost:4582");
        }
        catch (UnknownHostException e) {
            Assertions.fail();
        }

    }
}*/
